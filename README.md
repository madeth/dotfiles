# Usage


```
  $ gem install homesick
  $ homesick clone madeth/dotfiles
  $ cd ~ && homesick symlink dotfiles
  $ mkdir mkdir -p ~/.vim/bundle
  $ git clone https://github.com/Shougo/neobundle.vim ~/.vim/bundle/neobundle.vim
  # :NeoBundleInstall
```
